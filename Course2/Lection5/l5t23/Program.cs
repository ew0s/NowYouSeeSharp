using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace l5t23
{
    /*
     * Один из классов наследников ведёт себя явно не адекватно! Проанализируйте код программы, найдите
     * наследника, который выбивается из общей картины и сделайте так, чтобы:
     *      1) Ни один из наследников класса Cat не мог переопределять метод Meow().
     *      2) Все текущие переопределения (если они имеются) должны быть удалены.
     */

    public class CatStore
    {
        public static void Main(string[] args)
        {
            /* Добавьте свой код ниже */
        }
    }
}
